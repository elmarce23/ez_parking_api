const config = require("./db");
const sql = require("mssql");

async function newTicket(ticket) {
  try {
    
  } catch (error) {
    console.log(error);
  }
}

// Exporting modules
module.exports = {
  newTicket: newTicket
};
